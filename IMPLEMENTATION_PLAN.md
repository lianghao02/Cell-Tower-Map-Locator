# 實作計畫

## 目標與驗收條件

- 讓扇形交集明確呈現為依基地台座標、方位角與顯示扇區參數計算的分析參考範圍，不描述為手機實際或精確位置。
- 交集卡片顯示使用扇區數、分析方式、結果狀態與限制；無交集顯示為分析結果，不視為程式錯誤。
- 調閱回覆的批次提示能區分定位完成、無法定位、可繪製基地台與缺少有效基地台座標的資料列。
- 分享 Hash 只接受及產生非敏感的地圖座標狀態；不得讀取或寫入門號、時間或地址名稱。
- 維持 Leaflet 幾何演算法、既有解析相容性與歷史資料行為。

## 不做範圍

- 不更換前端框架、GIS 函式庫或交集演算法。
- 不加入推定距離、定位機率、信心分數或任何未具來源依據的定位精度。
- 不新增雲端服務、帳號、案件管理或複雜圖層控制器。

## 現況與限制

- 幾何交集使用既有 Sutherland-Hodgman 多邊形剪裁；扇區半徑與夾角為使用者可調整的顯示／分析參數。
- 多筆資料可由清單、Marker、扇區與歷史多選對應；交集目前僅以紅色熱區與「核心熱點」呈現，說明不足。
- `getAppLink()` 不寫入門號與時間，但 `checkUrlParams()` 仍會讀取門號、時間與地址名稱，需移除。

## 已確認決策

- 使用「訊號交集分析參考範圍」與「幾何中心參考點」取代可能被誤解為實際定位的名稱。
- 批次資料品質僅就來源可判定的狀態分類；方位角缺失保留資料列，但明確提示其不參與扇區交集。

## 工作清單

- [x] 建立來源／正規化／地圖／分析／分享資料流的最小說明｜程式碼追查完成
- [x] 修正 Hash 讀取與產生的敏感欄位防線｜靜態檢查完成
- [x] 增加調閱回覆資料品質摘要｜解析邏輯檢查完成
- [x] 更新交集卡片、Tooltip、Popup 與按鈕文字的真實性邊界｜DOM 與字串檢查完成
- [ ] 驗證解析、Marker、扇區、交集與無交集狀態｜待提交後公開版實測

## 風險與因應

- 舊分享連結中的方位角或地址關聯不再還原：這是排除非必要資料的既定隱私邊界；保留基地台座標可開圖。
- 缺少方位角的資料不繪製扇區：維持既有行為，只增加狀態說明。

## 驗證紀錄

- `node --check js/app.js` 通過。
- `scripts/qa.ps1` 通過。
- `git diff --check` 通過。

## 剩餘問題

- 公開版實測需在使用者授權推送後進行；本輪尚未推送。
